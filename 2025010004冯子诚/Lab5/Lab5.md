# Lab5：从固定容量类到 C++ 动态扩容向量

## 一、配置 CMakeLists.txt
（已根据作业要求配置，见 CMakeLists.txt 文件）

## 二、完成本次作业所需的新知识
（已理解相关概念）

## 三、intvector.cpp：完成动态整数向量
（已完成编写，见 intvector.cpp 文件）

## 四、选择题与填空

### 4.1 `size` 与 `capacity`
- [x] B. `0` 到 `2`

### 4.2 动态数组的释放
- [x] C. `delete[] p;`

### 4.3 初始容量为零
- [x] B. `size == 0`、`capacity == 1`

### 4.4 扩容的执行顺序
- [x] C. 申请新数组 → 复制有效元素 → 释放旧数组 → 更新指针和容量

### 4.5 扩容会不会增加元素个数
- [x] C. `size == 4`、`capacity == 8`

### 4.6 为什么先检查下标
- [x] A. 返回 `false`，`size` 仍为 `4`，`capacity` 仍为 `4`

### 4.7 析构函数
- [x] D. 离开 `v` 所在作用域时自动执行析构函数，由本例函数体释放当前数组

### 4.8 禁止复制对象的原因
- [x] B. 避免直接复制指针后两个对象共用数组，造成失效访问或重复释放

### 4.9 末尾追加的复杂度
- [x] C. 发生扩容的单次追加为 `O(n)`；从空表连续追加时，每次均摊为 `O(1)`

### 4.10 扩容与中间插入
- [x] B. `O(n)`，仍可能移动大量元素；扩容复制与右移两段线性工作相加仍为线性

### 4.11 填空：程序运行验证
- [x] A. 一致
- 填空内容：无

### 4.12 填空：容量变化记录

| 观察位置 | 操作后的 `size` | 操作后的 `capacity` | 本步扩容复制个数 |
| :--- | :---: | :---: | :---: |
| `IntVector v;` 创建后 | 0 | 4 | 0 |
| `v` 追加 `40` 后 | 4 | 4 | 0 |
| 满表 `v.insert(5, 99)` 失败后 | 4 | 4 | 0 |
| `v` 追加 `50` 后 | 5 | 8 | 4 |
| 满表 `v.insert(4, 28)` 后 | 9 | 16 | 8 |
| 六次删除完成、`v` 删除全部元素后 | 0 | 16 | 0 |

### 4.13 填空：解释扩容过程
用两三句话说明：为什么扩容时要先复制再释放旧数组？为什么扩容后不能直接把 `size` 设成 `capacity`？
回答：扩容时必须先复制再释放旧数组，因为如果先释放，旧数组的数据就会丢失，无法复制到新数组中。扩容后不能把 size 设为 capacity，因为扩容只是增加了底层数组的容量，并没有新增有效元素。size 表示当前有效元素的个数，扩容本身不改变有效元素的数量，新元素是在后续的 insert 或 pushBack 操作中才会写入并增加 size 的。

---

### 3.6.4 必填：Memory View 观察记录

完成三张截图后，立即把自己环境中的观察结果填入下表。这一部分与三张截图同属必交内容，随复制到个人目录的 `Lab5.md` 一起提交。地址记录为实际看到的十六进制值；图 A 的未初始化变量和图 C 已离开作用域的变量无需读取。

| 记录项目 | 图 A：扩容前 | 图 B：复制完成 | 图 C：追加完成 |
| :--- | :--- | :--- | :--- |
| 当前 `data` 地址（图 C 为 `v.data`） | `0x0000000000fc17f0` | `0x000000000010a1830` | `0x000000000010a1830` |
| `newData` 地址 | 尚未申请，不填写 | `0x000000000010a1830` | 已离开作用域，不填写 |
| 当前对象的 `size` | 4 | 4 | 5 |
| 当前对象的 `capacity` | 4 | 8 | 8 |

填写自己环境中的大小和计算结果：

- `sizeof(int)`： （填写）字节。
- 容量为 `4` 时，整数元素存储区大小： （填写）字节。
- 容量为 `8` 时，整数元素存储区大小： （填写）字节。

如果 Memory View 在 `delete[]` 之后仍显示旧数组的字节，是否可以继续在程序中访问旧数组？说明理由。

>不可以。即使 Memory View 仍然显示旧数组的字节数据，程序也绝对不能继续访问该旧数组，在 delete[] data; 执行后，必须立即将 data 指针指向新申请的内存（即代码中的 data = newData;）。任何试图通过旧地址访问数据的行为都是非法的，即便 Memory View 中看起来数据还“在那里”。 在此填写你的回答（不少于 20 字）。